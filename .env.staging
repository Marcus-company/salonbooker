# Staging/Acceptance Environment
NODE_ENV=staging
NEXT_PUBLIC_SUPABASE_URL=https://staging-abc123.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=${STAGING_SUPABASE_ANON_KEY}
SUPABASE_SERVICE_ROLE_KEY=${STAGING_SUPABASE_SERVICE_KEY}

# App Settings
NEXT_PUBLIC_APP_URL=https://staging.hairsalonx.nl
ADMIN_EMAIL=admin@hairsalonx.nl

# Email Configuration (use real SMTP provider)
SMTP_HOST=${STAGING_SMTP_HOST}
SMTP_PORT=${STAGING_SMTP_PORT}
SMTP_USER=${STAGING_SMTP_USER}
SMTP_PASS=${STAGING_SMTP_PASS}
FROM_EMAIL=noreply@hairsalonx.nl
FROM_NAME="HairsalonX Staging"

# Logging
LOG_LEVEL=debug
